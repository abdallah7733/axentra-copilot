Drop two files here (both optional):

- call.mp3     The generated call dialogue. Starts at the "incoming call" scene.
               Generate it with docs/call-audio-prompt.md so the timing matches.
- ambient.mp3  Low ambient bed, loops under the whole presentation.
               If missing, a soft synthesized pad plays instead.

Presentation Mode picks them up automatically. Nothing else to configure.
